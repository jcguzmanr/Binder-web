# Contact and other site forms
# VITE_WEBHOOK_URL=

# Event / webinar — workflow Bubble `evento-de-cierre` (POST sin /initialize; el cliente lo quita si viene en la var).
# Payload: firstName, lastName, email, jobTitle, company, phone, phoneCountry, consent, timestamp.
VITE_EVENTS_WEBHOOK_URL=https://binder0.bubbleapps.io/version-test/api/1.1/wf/evento-de-cierre

# LinkedIn Insight Tag — only loads on event pages when set
# VITE_LINKEDIN_PARTNER_ID=

# Contact and other site forms
# VITE_WEBHOOK_URL=

# Event / webinar registration (POST JSON from /eventos/:slug)
# En Bubble el workflow debe estar expuesto para la API (“initialization” / público según tu plan).
# Si ves 404 con mensaje "not in initialization mode", habilita el endpoint en el editor de Bubble.
VITE_EVENTS_WEBHOOK_URL=https://binder0.bubbleapps.io/version-test/api/1.1/wf/binderla-formulario/initialize

# LinkedIn Insight Tag — only loads on event pages when set
# VITE_LINKEDIN_PARTNER_ID=

# Contact and other site forms
# VITE_WEBHOOK_URL=

# Event / webinar registration (POST JSON from /eventos/:slug)
# Usar .../wf/{nombre} sin sufijo /initialize (el código lo quita si viene en la variable).
VITE_EVENTS_WEBHOOK_URL=https://binder0.bubbleapps.io/version-test/api/1.1/wf/binderla-formulario

# LinkedIn Insight Tag — only loads on event pages when set
# VITE_LINKEDIN_PARTNER_ID=
